import Layout from "../components/Layout";

const LiterarySeries = () => (
  <Layout title="Les séries littéraires">
    <h1>Page des séries littéraires</h1>
  </Layout>
);

export default LiterarySeries;
